// require the Util and fs needed
//util is a build in feature of node like fs
const util = require('util');
const fs = require('fs');

const uuidv1 = require('uuid/v1');

//writeToFile
//readToFile

class Store {
    read() {

    }
    write() {

    }

    //get the notes
    //add some notes
}

module.exports = new Store();